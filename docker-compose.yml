version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: vlr-scraper
    command: python main.py
    # volumes:
    #   - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env